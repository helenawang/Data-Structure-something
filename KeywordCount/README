KeywordCount是一个用于统计源程序中各保留字出现次数的小程序。目前支持C语言（C89）。

程序使用说明：
1. 可将src/util目录下的Count.java作为程序入口，在main方法的第25行Preprocess构造函数中设置好待分析的源程序文件的路径，即可运行。
2. 关键字表存放在data/keywords.txt文件中，目前只包含C89的32个保留字，按字典序排列。

程序实现说明：
1. 目前是把关键字建成HashMap，key为对关键字字符串进行散列得到的整数，value为关键字的附加信息。
2. 散列函数的设计受Karp-Rabin算法的启发，把待散列的字符串（由26个小写字母构成）看成一个26进制数，从而转换为long型十进制整数，得到散列值（信息指纹）。

目前的版本只是一种简易方法的尝试，效率还有提高的空间。另外，源程序预处理部分的健壮性也有待提高。
